<%= form_with(model: accident, local: true) do |form| %>
  <% if accident.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(accident.errors.count, "error") %> prohibited this accident from being saved:</h2>

      <ul>
      <% accident.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time, id: :accident_time %>
  </div>

  <div class="field">
    <%= form.label :accident_type %>
    <%= form.text_field :accident_type, id: :accident_type %>
  </div>

  <div class="field">
    <%= form.label :car_id %><br/>
    <%= form.select :car_id, options_for_select(Car.all.map{|s|[s.id.to_s + " " + s.car_type + " " + s.model]}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
